# KILO Strength Product Context

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户是已经有规律训练基础、希望精确执行力量或增肌计划的男性训练者。他们通常在健身房单手操作手机，需要快速选择训练计划、记录每组、完成休息计时，并查看动作识别反馈和长期进步。

## Product Purpose

KILO 将训练计划、极简组记录、动作识别、训练日历、训练记录和有来源的知识库问答放在一个本地优先的工具中。成功标准是用户能在不被多余信息打断的情况下，从计划详情进入训练并完成第一组，同时在训练后看懂计划与实际表现的差距。

## Positioning

动作识别是独立的一等入口，同时可以把可解释的动作质量反馈关联到训练动作或组。训练记录和推荐逻辑保持透明、可覆盖，不把用户交给不可解释的评分或社交内容。

## Operating Context

用户在健身房光线、网络和单手操作条件不稳定的情况下使用手机。训练页面需要大触控目标、低信息密度和明确的下一步；训练前后可以使用弹窗或详情页查看复杂设置。Web 原型用于验证移动端形态，正式端将进入 Flutter iOS/Android。

## Capabilities and Constraints

- 七个一级入口：主页、训练、记录、动作库、识别、AI、我的；“我的”集中训练设置、设备连接、通知与隐私入口。
- 支持已保存计划、空白训练、动作库、自定义动作、组类型、休息计时、日历排程、训练记录和进步图表。
- 计划详情不会自动启动计时，只有明确点击开始训练才进入实时训练。
- 组类型必须通过选择器切换，不能依赖点击图标循环切换。
- 当前 Web 原型使用 Vite、TypeScript、原生 HTML/CSS 和 localStorage；不接入生产认证或持久化云端训练数据。
- 动作素材复用会话 `019fa168-db20-7ff0-a031-267e32d6caa6` 的许可素材。

## Brand Commitments

产品临时命名为 KILO Strength。视觉需要浅色、专业、硬核但克制，避免女性照片日记应用的品牌和数据模型。使用真实动作素材、Phosphor 图标和单一蓝色行动色，不使用 emoji 作为结构图标。

## Evidence on Hand

- `web-prototype/src/data.ts` 中有 32 个动作定义和官方训练计划。
- `web-prototype/public/assets/exercises/` 中有动作素材和许可声明。
- 现有 Web 原型已具备训练、记录、日历、动作识别和 AI 的交互状态。
- 当前没有目标用户实际使用数据，视觉判断以已给出的需求、成熟健身软件模式和可用性原则为依据。

## Product Principles

1. 先开始第一组，再展开复杂设置。
2. 训练中只显示当前动作、当前组和下一步。
3. 所有自动建议都说明依据，并允许覆盖。
4. 数据可恢复、可编辑、可解释，不把状态隐藏在装饰里。
5. 动效表达状态和进步，减少动态效果时仍然完整可用。

## Accessibility & Inclusion

移动触控目标至少 44px，键盘和屏幕阅读器可操作；颜色不作为唯一状态信号；支持 200% 文本缩放、低置信度/离线/空数据状态和 `prefers-reduced-motion`。

## Open Decisions

- 当前没有真实用户测试数据，最终视觉密度和文案仍需通过 8–12 名有训练基础用户验证。
- React Bits 是 React 组件库而非 Codex Skill；当前项目保持原生 TypeScript/Vite，动效将使用其公开组件思路的轻量 CSS/Canvas 实现。
