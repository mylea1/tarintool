# 训练明细卡片与计划文件夹（1.0.39+41）

- 好友计划展示动作缩略图和逐组重量、次数、时长。保存后按钮与提示显示“已保存”，当前页面防止重复保存；默认保存至未分类。
- 计划列表支持全部、未分类、自建文件夹筛选。更多操作可以移动计划；删除文件夹只移除归类，不删除训练计划。已有分类保留，新保存的官方、AI、好友、自建计划默认不自动创建分类。
- 结束训练可选择保存为计划及可选文件夹，默认不放入文件夹。计划保存使用本次实际重量；历史记录独立保留。
- 完成、日历、记录列表及动态使用品牌标识与动作明细为主的卡片。汇总数据放底部；明细不展示私人备注。记录详情保留用户自己的备注与原有每组数据。
- 完成页删除分享卡片入口，保留分享到动态；发布页直接预览明细并填写可选文字，不再显示仅用于本机海报的照片与配色选项。
- 动态接口新增经过白名单清洗的 setDetails，旧动态没有明细时只展示已有组数/最高重量，不反推或伪造每组记录。动态互动控件位于明细卡片之外。

## 服务端

只对线上 workoutExerciseSnapshot 增加 setDetails 白名单，与仓库其他后端差异隔离。

- 原 release：`/opt/kilo/releases/navigation-id-20260905`
- 新 release：`/opt/kilo/releases/training-cards-20260906-v41`
- 线上副本全部 59 项测试通过后切换 `/opt/kilo/current`，重启 `kilo-api`，健康检查 HTTP 200。
- 无数据库迁移。回退可把 current 指回原 release 后重启；不要恢复覆盖业务数据库。

## 验证

本次增加计划可选归类/删除保留/好友保存独立副本、320/375/414 宽度及 200% 字体明细布局测试；完成页测试验证直接显示明细与唯一动态分享入口。

`test/training_card_visual_capture.dart` 可独立渲染亮暗卡片检查。APK 按 `docs/android-isolated-build-runbook.md` 使用提交快照隔离构建，并执行全量质量检查。
