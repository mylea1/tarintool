# 动作排序与分类标签：02 / 10 实施记录

范围：按用户要求仅实现完整动效文档的 02 拖拽排序和 10 分类标签。直接修改 Flutter，不维护 HTML，不打包。

## 排序

- 实时训练、计划编辑共用 ExerciseReorderList 和独立的 44dp 拖动把手，输入框和卡片正文不启动拖动。
- 原生 ReorderableListView 的 onReorderItem 使用已调整后的目标索引，不再减一；代理最多放大 1.02 并增加轻微阴影。减少动态效果时取消代理缩放与阴影。
- 单一滚动容器支持长列表边缘自动滚动。以动作实例 ID 为身份；同一 supersetId 整体移动，原上移/下移菜单沿用整体移动语义。
- 松手后一次提交 ID 序列：实时训练保存活动快照，计划编辑只改草稿，保存按钮才落库。不改组数据、计时及 AI 上下文。
- 拖动开始隐藏文字选择浮层，保留输入值、选择和焦点，防止 Flutter 拖动代理与选择浮层层级冲突。
- 修复快速连续添加动作时微秒时间戳重复的问题，对已存在实例 ID 增加冲突后缀。

## 标签

- 新增 MotionFilterTag，160ms 配色及真实内边距过渡，选中项扩宽会推动邻项，不使用覆盖邻项的 Transform.scale。
- 动作库器械筛选采用横向可滚动标签，选择立即更新结果，可连续比较，点击完成关闭。
- 动作库部位栏、添加/替换动作中的部位和器械栏使用同一选中语义与主题色。遵循文档固定 rail 约束，栏宽不扩大、不挤压右侧动作列表。
- 减少动态效果时标签即时更新；保留单选、全部、原有过滤与动作选择逻辑。

## 验证

当前工作区 flutter analyze 无问题；完整 flutter test：294 通过、1 跳过。
新增测试覆盖首项拖到末项、字段状态保留、原对象与超级组关系、计时不变、非法顺序拒绝、快速添加实例唯一性，以及 320/375/414 宽和 200% 字体下标签不重叠。
更新原生列表手势定位测试，并覆盖开启语义时修改休息的流程。
使用 Flutter 测试渲染采集了浅深主题实时训练布局。测试字体和部分异步素材与真机不同，不能当商店宣传素材；未进行真机帧率或设备读屏操作验收。

本次提交只包含本任务代码和测试；工作区原有品牌卡、版本号和其他界面改动保持未提交。测试结果属于当时完整工作区，不将这些原有改动声称为本次实现。
