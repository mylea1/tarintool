# 数据契约冻结门

本目录预留给 Web 目标用户测试通过后的正式 JSON Schema、枚举、API 请求/响应和测试夹具。

当前不从高保真原型直接冻结生产契约。进入 Flutter 实现前必须先完成：

1. 8–12 名有训练基础男性的核心任务测试。
2. 修订训练组、休息计时、计划排程、识别报告和 AI 授权交互。
3. 明确稳定 UUID、修订号、删除标记和设备标识字段。
4. 冻结状态机与跨平台测试夹具。

这样可以避免把未验证的 Web 交互细节固化进 Drift 数据库、ActivityKit、WatchConnectivity 和 Android 前台服务。
